import { ModulePlaceholder } from '@/components/erp/ModulePlaceholder';

export default function BomPage() {
  return (
    <ModulePlaceholder
      title="Bill of Materials"
      phase="Phase 2"
      description="The recipe per product — the fixed list of materials and quantities needed to build one pallet."
    />
  );
}
